#!/bin/bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")" && pwd)"

if [ -x "$ROOT_DIR/.forge/scripts/forge" ]; then
  exec "$ROOT_DIR/.forge/scripts/forge" "$@"
fi

echo "forge: .forge/scripts/forge is missing." >&2
exit 1
